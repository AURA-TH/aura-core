# @aura/workflow

Task, Approval, and orchestration logic that sequences AI and human steps.

## Boundaries

Depends on `database` and `shared`. May coordinate `actions`. Must not import from `apps/*` or `ui`.

> Sprint DEV-001: placeholder only. No implementation in this sprint.
