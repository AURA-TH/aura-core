# @aura/memory

AI Memory and the Business Memory Graph — long-term context the brain reasons over.

## Boundaries

Depends on `database` and `shared`. Must not import from `apps/*` or `ui`.

> Sprint DEV-001: placeholder only. No implementation in this sprint.
