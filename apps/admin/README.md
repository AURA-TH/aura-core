# @aura/admin

Internal admin / operations console. Built with Next.js + TypeScript.

## Boundaries

Depends on `packages/*`. Must not depend on `apps/web` or `apps/api` source directly.

> Sprint DEV-001: placeholder only. No implementation in this sprint.
