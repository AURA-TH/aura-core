# @aura/web

Store owner-facing dashboard for the AI Customer Service Employee. Built with Next.js + TypeScript.

## Boundaries

Depends on `packages/*`. Must not depend on `apps/admin` or `apps/api` source directly — talks to the backend over its API.

> Sprint DEV-001: placeholder only. No implementation in this sprint.
