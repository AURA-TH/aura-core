# @aura/api

Backend modular monolith built with NestJS + TypeScript. Hosts MVP domains (Auth, Workspace, Product, FAQ, Customer, Conversation, Message, AI Reply, AI Memory, AI Action, Approval, Task, Audit Log, Dashboard Today) in future sprints.

## Boundaries

Depends on `packages/*`. The single deployable backend; domain modules are internal boundaries, not separate services.

> Sprint DEV-001: placeholder only. No implementation in this sprint.
